/**
 * Language-specific syntactic rules for Basque (Euskara).
 * Currently no specific auto-corrections are defined.
 */
export function applyPedagogicalRules(tagId: string, functionTagId?: string) {
  return { finalTagId: tagId, finalFunctionTagId: functionTagId };
}
